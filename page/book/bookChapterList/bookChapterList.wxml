<view class="chapter" wx:for="{{chapters}}" id="{{index}}" wx:key="*key">
    <navigator url="../readView/readView?chapterid={{item.id}}&chapter_name={{item.chapter_name}}&bookid={{bookid}}&position={{item.position}}"  hover-class="navigator" hover-stay-time="100">
        <view >{{item.chapter_name}}</view>
    </navigator>
</view>