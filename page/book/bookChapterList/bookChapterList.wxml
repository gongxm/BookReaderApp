<view class="chapter" bindtap="navigateToBook" wx:for="{{chapters}}" id="{{index}}" wx:key="*key">
    <navigator url=""  hover-class="navigator" hover-stay-time="100">
        <view >{{item.chapter_name}}</view>
    </navigator>
</view>